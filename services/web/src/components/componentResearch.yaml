Components:
  
  1. NavBar:
      - header title 
      - home
      - sign up
      - login 
      * hello <user>
      * account (settings sign)
      * logout
    conditional rendering:
      - Vuex

  2. Header:
    grid:
      * 3 columns
      * 1 row
    condition:
      1. not logged in 
          column 2/3:
            - title VoteApp
            - titletext "Create custom polls with live results"
            - signup button:
              * button
              * href
      2. logged in
          column 2/3:
            - title "dashboard"
            - p "what would you like to do today?"
            - buttons:
              * new poll
              * my polls

  3. Home:
      grid:
        * 3 columns
        * 1 row
          column 1/3:
            - thunder icon
            - h2 "Live Results"
            - p "Live graphs show your results immediately"
          column 2/3:
            - globe icon
            - h2 "Works Everywhere"
            - p "a responsive desing that works on tablets, smartphones and web"
          column 3/3:
            - FB icon
            - h2 "Social Integration"
            - p "free included Facebook comments allow your public to immediately comment"
  4. Sign Up:
       grid:
        * 3 columns
        * 1 row
        column 2/3:     
          form:
            - name field:
              * p "name"
              * <input>  field
            - email field:
              * p "email"
              * <input> field
            - password field: 
              * p "password"
              * <input> field
            - sign up button:

  5. Login:
      grid:
        * 3 columns
        * 1 row
        column 2/3: 
          form:
            - email field:
              * p "email"
              * <input> field
            - password field:
              * p "password"
              * <input> field

  6. Create poll:
    grid:
      * 3 columns
      * 1 rows
      column 2/3:
        - h2 "New Poll" 
          form:
            * poll name field:
              -  p "Name your poll"
              - <input> (suggest "Best soft drink")
            * Options fields:
              - p "Options"
              - options.forEach((option) => {<input> })
            * More Options Button:
            * Submit Button:
  7. Poll: 
    grid:
      * 3 columns
      * 1 rows
    column 1/3:
      - h2 <poll title>
      - p by <creator> 
    column 2/3:
      - options:
        options.forEach((options) => {
          * checkmark
          * p <options>
        })
      - vote button:
    column 3/3:
      - p 'Sorry, comments is no MVP feature yet'

  8. Poll Result:
      grid:
        * 3 columns
        * 1 rows
      column 2/3:
        - h2 <poll question>
        - chart.js results 

  9. My polls:
      grid:
        * 3 columns
        * 1 rows
      column 2/3:
        polls.forEach((poll) => {
          - p poll
          - delete button
        })

  10. account:
       grid:
          * 3 columns
          * 1 rows
        column 2/3:
          - h2 Change Password
          form:
            * current password
              - p "current password"
              - input field
            * new password
              - p "new password"
              - input field
            * button
              - "Save Changes"





     

